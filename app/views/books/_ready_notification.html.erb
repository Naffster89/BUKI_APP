<turbo-stream action="append" target="notifications">
  <template>
    <div class="alert alert-success d-flex justify-content-between align-items-center mt-3">
      <span>✅ Your book "<%= book.title %>" is ready!</span>
      <%= link_to "View it", book_path(book), class: "btn btn-sm btn-light" %>
    </div>
  </template>
</turbo-stream>
